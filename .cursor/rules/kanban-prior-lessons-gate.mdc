---
description: Kanban pre-implementation — consult done-card lessons and commit-issue patterns before Decisions
globs: .devtool/features/**/*.md
alwaysApply: false
---

# Kanban prior lessons gate

When running **pre-implementation card review** on a **feature**, **bug**, or **agent** card — or **review** on a **`commit-issue`** card — follow [kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md) § **Prior lessons gate** before writing **Decisions** / **Corrective Action**. Signature: `lessons-by-area-routing` — index + routing table before broad done-card grep.

## Required

1. Read **`docs/lessons-index.yaml`** for each resolved area label (grouped Signatures + artifact paths) when the index exists locally, and the matching row in [agent-triage/reference.md](../skills/agent-triage/reference.md) § **Lessons by area** — before broad done-card grep. Index paths prefer structured ``artifacts:`` sub-bullets on done cards (`parse_artifacts_line` in `scripts/resolve_prior_lessons.py`); inline `` `sig:slug` `` on lesson bullets is indexed too (Signature: `lessons-index-inline-sig-backtick`); fall back to **Governance** link heuristics when absent.
2. Optional: `python3 scripts/resolve_feature_areas.py --lessons "<Area>"` — registry `lesson_signatures` / `lesson_docs` pointers only.
3. Run `python3 scripts/resolve_prior_lessons.py` with card **`epic`**, **`## Feature Areas`** /
   **`## Feature Area`** labels, and optional path prefixes from **Product Paths**, legacy **Label
   Paths**, or **Tests → Files** (merged by `extract_label_paths` — Signature:
   `kanban-card-scope-schema`). Output includes **Registry lesson pointers** when
   `lesson_signatures` / `lesson_docs` are set on resolved areas (Signature:
   `feature-areas-lesson-pointers`).
4. Read **`## Lessons captured`** on matched **`done/`** and **`archived/`** cards only when index + resolver output are still ambiguous; skim open **`commit-issue`** cards with overlapping staged paths.
5. Load area **skills & rules** from [AGENTS.md](../../AGENTS.md) § Area → skills & rules; grep **Signatures** in [agent-triage/reference.md](../skills/agent-triage/reference.md) § Failure pattern routing when symptoms match.
6. **Record** applied lessons on the card — one bullet under **Decisions** or **Corrective Action** (`**Prior lessons (YYYY-MM-DD):**` …) with links to done/archived cards / Signatures used.

**Coverage (C4):** citations feed **Application Coverage** — composite and 75% drift use **per-card**
C4 (`audit_application_coverage_per_card`: ≥1 accepted cite on eligible active cards); **aggregate**
C4 (cited/total surfaced) is advisory only — [docs/governance/lessons-and-coverage.md](../../docs/governance/lessons-and-coverage.md)
§ **Lessons Coverage Metric**; audit via `check_lessons_coverage.py` or
`resolve_prior_lessons.py --audit application` (application mode uses per-card score).
Signature: `docs-governance-split`. **`**Prior lessons (YYYY-MM-DD):**` block:** parser SSOT in `resolve_prior_lessons.py` (`PRIOR_LESSONS_RE`) — stops at the next `##` heading only (mid-block `**` lines such as `**QA follow-up**` do **not** truncate the block). Accepted cite formats:

| Pattern | Example |
| ------- | ------- |
| Done / archived card stem | `feature-foo-2026-06-27.md`, commit-issue `…T143059.md` |
| Drift spawn stem | `governance-drift-registry-{hash}.md` |
| Signature backtick | `` `lessons-coverage-c2-c3-audit` ``, `` `sig:slug` `` in prose |
| Governance path backtick | `` `.cursor/rules/testing.mdc` ``, `` `docs/governance/lessons-and-coverage.md` `` |

Signature: `lessons-coverage-c2-c3-audit`, `lessons-coverage-c1b-forward-feedback`,
`lessons-coverage-c4-per-card-threshold` ([testing.mdc](testing.mdc)).

## Do not

- Skip the gate because the card is small or epic-linked Context already names a parent card
- Implement before **Decisions** / **Corrective Action** when lessons change the plan
- Duplicate **Fix pattern** prose from reference tables on the card — cite **Signature** or done-card path only

## Full workflow

[kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md) § Pre-implementation card review + § Prior lessons gate.
