---
description: Kanban inquiry cards — research Response, spawn feature cards from recommendations
globs: .devtool/features/**/*.md
alwaysApply: false
---

# Kanban inquiry cards

`labels: ["inquiry"]` — [kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md) § Inquiry cards.
Signature: `kanban-cursor-mode-gates`.

**User:** **Description**; optional **Feature Areas**. **`Inquire @card`** + Ask Mode — chat only;
**Response** on card after **`update`** / rare `review and update`.

**Agent (optional):** Product scope when areas set. **Spawn:** [SKILL.md § Spawn from inquiry](../skills/kanban-markdown/SKILL.md#spawn-from-inquiry) —
`todo` children with `epic` + review-ready sections; parent **`## Spawned feature cards`** table.

**Done:** move only — no lessons or ff ([kanban-card-gates.mdc](kanban-card-gates.mdc) §4).

**Do not:** **Decisions** / **Corrective Action** / **AC** on inquiry card; **Response** during
`Inquire` alone; Backlog spawns; implement on inquiry unless user asks.
