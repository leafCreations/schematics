---
description: System prompt wrapper — kanban-first routing, skill/rule load order, turn lifecycle
alwaysApply: true
---

# Agent routing (system wrapper)

**Repo entry:** [AGENTS.md](../../AGENTS.md). **Do not** treat [docs/roadmap.md](../../docs/roadmap.md) as the queue.

**Hard gates:** [kanban-card-gates.mdc](kanban-card-gates.mdc) — kanban card required for implementation; valid frontmatter `labels` required.

## User mode

| Mode | Work source | Code / cards |
| ---- | ----------- | ------------ |
| **Agent** | Card named + **agent verb** — `implement`, `update`, `review and update`, `spawn`, `answer inquiry`, … | [kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md); [kanban-card-gates.mdc](kanban-card-gates.mdc) §2 |
| **Ask-only** | `review …` card only, bare `@path`, no card, or no agent verb | Read-only — no file edits |

## Turn lifecycle

```text
START → agent-triage/SKILL.md (classify; card + label + prompt-verb gate; one scoped kanban-*-cards.mdc; §1b on failure signals only)
WORK  → card Label Paths + Label Methods → prior lessons gate (feature/bug/agent/commit-issue)
        → Decisions / Corrective Action → implement
        → Review QA fix: **QA follow-up** + refresh Feature Areas / Label Paths / Label Methods
VERIFY → ruff E501 on touched `.py`/`.pyi` before handoff; targeted pytest; pre-commit-pytest.sh before Review/commit
DONE  → user says card Done → lessons + forward feedback **only** for feature / bug / agent / commit-issue
        → top-3 forward feedback in chat before handoff; inquiry Done: **no** lessons or forward feedback
        → build_lessons_index.py when lessons ran; curate feature-areas lesson_* when applicable
END   → agent-self-evaluation/SKILL.md + handoff block
```

**Implementation / fix / refactor:** edit **≥1** `.cursor/skills/*` and **≥1** `.cursor/rules/*.mdc` before handoff (different learnings — skill = workflow, rule = constraint).

## Kanban card type (read `labels` in frontmatter)

| Label | Rule | Agent plan section |
| ----- | ---- | ------------------ |
| `feature` | [kanban-feature-cards.mdc](kanban-feature-cards.mdc) | **Decisions** |
| `bug` | [kanban-bug-cards.mdc](kanban-bug-cards.mdc) | **Corrective Action** (not Decisions) |
| `commit-issue` | [kanban-commit-issue-cards.mdc](kanban-commit-issue-cards.mdc) | **Review** → Root Cause + Corrective Action; implement after user approval |
| `inquiry` | [kanban-inquiry-cards.mdc](kanban-inquiry-cards.mdc) | **Response**; spawn child cards when user approves; **no** Card Done lessons |
| `agent` | [kanban-agent-cards.mdc](kanban-agent-cards.mdc) | **Decisions**; user **Description** + **Feature Area** |
| **Invalid** (missing / `[]` / unknown) | [kanban-card-gates.mdc](kanban-card-gates.mdc) | **Stop** — user must fix `labels` |

Resolve **Feature Areas** (or agent card **Feature Area**) → **Label Paths** + **Label Methods** via `docs/feature-areas.yaml` before `in-progress`.

## Code style (Python)

All agent edits to `.py` / `.pyi` files must keep lines ≤ **100** characters (`pyproject.toml`
`line-length = 100`). **Before handoff** on implementation turns, run
`.venv/bin/ruff check --select E501` on every touched path — Signature: `ruff-e501-line-length`.
Pre-commit ruff is hook 1; E501 failures must not reach commit. Wrap strings, split imports,
break call chains while editing — not only after hook failure.

## Load skills (on demand — not all every turn)

| Touching | Read first |
| -------- | ---------- |
| Unfamiliar layout | [repo-map/SKILL.md](../skills/repo-map/SKILL.md) |
| Kanban card | [kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md) |
| UI | [ui-change/SKILL.md](../skills/ui-change/SKILL.md) + matching `ui-*.mdc` |
| Tests / commit | [targeted-testing/SKILL.md](../skills/targeted-testing/SKILL.md) |
| Docs after code | [docs-maintenance/SKILL.md](../skills/docs-maintenance/SKILL.md) |
| Minecraft / versions | [project-context/SKILL.md](../skills/project-context/SKILL.md) |

## Discovery budget

- **Kanban + Feature Areas** → `docs/lessons-index.yaml` area block + [agent-triage/reference.md](../skills/agent-triage/reference.md) § Lessons by area before broad `.devtool/features/done/` grep ([kanban-prior-lessons-gate.mdc](kanban-prior-lessons-gate.mdc))
- **Failure signals** (hook, pytest, UI wiring, handoff) → [agent-triage/SKILL.md](../skills/agent-triage/SKILL.md) §1b grep before broad reads
- **Governance edits** → [agent-triage/reference.md](../skills/agent-triage/reference.md) § Consistency matrix
- **Governance audit card** → [kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md) § Periodic AGENTS.md governance audit (reference § Classify)
- **Classify signals** — canonical [agent-triage/reference.md](../skills/agent-triage/reference.md) § Classify; AGENTS § Classify quickly ≤5-row summary only; new signal → reference + bump `REFERENCE_CLASSIFY_FINGERPRINT` — Signature: `governance-compact-classify-ssot`
- **Grep** before opening `ui/main_window.py` or other large files
- After **3** full file reads → prefer grep or semantic search
- **No card or review-only prompt** → Ask-only — do not edit (Signature: `kanban-prompt-ask-vs-agent`)

## Handoff (last sections — every response)

1. **`### Files used`** — load order; role tags — see [agent-self-evaluation/SKILL.md](../skills/agent-self-evaluation/SKILL.md) §7
2. **`### Self-evaluation`** — compact one-line fields; **Context load** + **AGENTS.md** freshness;
   `Skills updated:` / `Rules updated:` on implementation turns. Signature:
   `governance-compact-self-eval-handoff`. See [agent-self-evaluation.mdc](agent-self-evaluation.mdc).

**[AGENTS.md](../../AGENTS.md):** evaluate for staleness every turn; update when routing, card types, or handoff format changes.
