---
description: Kanban commit-issue cards — auto-captured pre-commit failures, review then implement
globs: .devtool/features/**/*.md
alwaysApply: false
---

# Kanban commit-issue cards

`labels: ["commit-issue"]` — auto-created by `scripts/on_pre_commit_failure.sh` on failed **`git commit`**
only (`PRE_COMMIT=1`) — Signature: `precommit-no-card-on-manual-hook`. **Ruff:** `ruffRules` frontmatter
+ **`## Ruff rules`** — Signature: `precommit-ruff-sim110`.

**Capture script:** **Problem**, **Failed Tests**, **Staged files**. **Agent on review:** Root Cause,
Corrective Action; optional Product / Tests / Docs. **Implement** after user approval — trivial hooks
may use **`review and implement`** — Signature: `commit-issue-review-and-implement-one-shot`.

**Card Done:** [kanban-markdown/SKILL.md](../skills/kanban-markdown/SKILL.md) § Card Done; promote
patterns to [pre-commit-workflow/reference.md](../skills/pre-commit-workflow/reference.md).

**Do not:** **`## Decisions`**; delete **Problem** / **Failed Tests**; bare **`implement`** without
prior review.
